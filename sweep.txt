
### 1. [clip] Grid tiles paint a square plate and a square selection veil outside their rounded shape while they fade in
    vrgallery/gridmodel.py:639   confidence high
    WRONG: The rounded clip is established only on the slow path (`if not cheap: p.setClipPath(path())`), because a cached `_tile()` pixmap already carries the rounded shape in its own alpha. But the fast path does not draw only that pixmap. Line 639 fills the WHOLE cell rect with surface2 as a backing plate for the fade, with square corners, at full opacity (setOpacity comes after the fill), and no clip is in force. The identical fill on the slow path at line 644 is clipped, which is exactly the asymmetry. With radius=12 that leaves a ~3.5 px triangular nub of #1c2130 at each of the four corners on top of the #0d0f15 page. Line 653 has the same hole with a different cause: the guard is `blit`, but the
    SEEN:  Every thumbnail, for the first 220 ms after it is decoded - i.e. on every scroll through un-cached photos, on first entry to Photos/Favorites/any drilled grid, and after a rescan. Tiles pop in as hard-cornered plates and then round off, and with a viewport of thumbnails arriving at once the whole grid does it together. The veil half additionally needs the tile to be selected (select-all, then scroll into fresh rows).
    FIX:   Set the clip once for the fast path too, rather than relying on the tile pixmap's own alpha for fills that are not the tile. Simplest: in the `cheap` branch, wrap the fade plate - `if alpha < 1.0: p.save(); p.setClipPath(path()); p.fillRect(r, QColor(style.PAL["surface2"])); p.restore()` - and change the overlay guard at line 653 from `if blit:` to `if cheap:`, which is the condition under which no clip has been set.

### 2. [overflow] The filter bar places 13 fixed-floor controls in a row nothing ever fits — below a ~1420 px window every field is squeezed under its own minimum
    C:\Users\Erik\Desktop\project\vrchronicle\vrgallery\gridpage.py:497   confidence high
    WRONG: Every item in this row has a hard width floor (three QComboBoxes floored by their longest entry, two QDateEdits by "yyyy-MM-dd" plus the calendar arrow, six QLabels, two buttons) and the count is fixed at 13. Measured layout minimum: 1074 px. The row's width is not its own choice — PageHead.place() sets the header to max(160, viewport.width() - 16), and PageHead.fit(), the machinery that drops and shrinks controls in a narrow window, walks only `self.row`; `set_extra_row` just does `self._col.addWidget(w)` and is never in the fitting at all. So nothing drops, nothing shrinks on purpose, and when 1074 > the width it has, Qt distributes the deficit by pushing every item BELOW its floor. Measur
    SEEN:  Photos page (also Favorites/any grid page), "More filters" toggled on, at any window narrower than about 1420 px. The app's own minimum is 840x470 and the comment at mainwindow.py:118 targets a 960-wide half-screen snap on a 1920 monitor -- at 960 the three combos and both date fields are 43 px wide. At the default 1500 window it fits, which is why the rendered screenshots do not show it.
    FIX:   Give the extra row the same degradation the header row has, or stop it needing one: either register the filter bar's groups with PageHead's drop/flex lists so fit() can shrink and drop them, or build the bar on widgets.FlowLayout (already in widgets.py:1430) so the From/Instance/Rating/Media groups wrap onto a second line instead of all shrinking past legibility. Whichever way, the combos need explicit setMinimumWidt

### 3. [overflow] A session card's top row is floored by the untruncated world name, so at ≤1100 px the Sessions page scrolls sideways and both action buttons sit off the right edge
    C:\Users\Erik\Desktop\project\vrchronicle\vrgallery\pages.py:383   confidence high
    WRONG: Six items, none of which elides, in a row whose width comes from the page. A plain QLabel's minimum IS its full text, and a VRChat world name is arbitrarily long, so the row's floor grows with the data and N never comes down. Measured on this library: title 258 + date 247 + instance badge 61 + count 106 + "Open all" 60 + "Contact sheet" 92, plus 5x10 spacing and the Card's 18+18 padding = 910 px per card; with the scroll body's 24+16 margins the Sessions holder's minimum floors at 952 px. The page is a QScrollArea with widgetResizable(True) and no horizontal-bar policy, so once the viewport drops under 952 the holder simply stops shrinking and a horizontal scrollbar appears. Measured: 1100 p
    SEEN:  Sessions page at any window under ~1130 px wide (Moments under ~980): a horizontal scrollbar under the cards, and "Open all"/"Contact sheet" plus the photo count hidden past the right edge until you scroll sideways. Worst on the long world names this library is full of ("VUdon Vehicles - Desert (Open Alpha)", "YTS 2.1 - YouTube Search, Subtitles...").
    FIX:   Make the title (and the date label) a widgets.ElidedLabel -- it exists for exactly this and reports a zero minimumSizeHint -- so the row's floor stops tracking the world name and the buttons keep their place. Same edit in MomentsPage._card.

### 4. [overflow] Five stat cards are laid in one row with a 174 px floor each, so under a ~1140 px window the Statistics page grows a horizontal scrollbar and the last cards fall off the edge
    C:\Users\Erik\Desktop\project\vrchronicle\vrgallery\pages.py:1032   confidence high
    WRONG: A StatCard's floor is its own content: 16+40+12 of icon and padding plus two non-eliding QLabels ("26 photos" at 20px bold over "busiest day · 2026-02-22" at 11px). Measured floor 174 px each; five of them plus 4x12 spacing = 870, plus the scroll body's 24+16 margins = 910 px that the holder can never go below. N is fixed at five whatever the width. The page is a QScrollArea with widgetResizable(True) and no horizontal-bar policy, so below a 910 px viewport the holder stops shrinking and the page starts scrolling sideways -- taking the month chart and every card below it with it. Measured horizontal scrollbar range: 0 at a 1500 and 1280 window, 72 at 1100, 212 at 960, 332 at 840. Card geomet
    SEEN:  Statistics page at any window under about 1140 px wide -- a half-screen snap on a 1920 monitor (960), which mainwindow.py:117 names as a supported size, loses the "people with you" and "busiest day" cards behind a horizontal scrollbar that also drags the charts sideways. Invisible at the 1500 default the screenshots were taken at.
    FIX:   Let the count come down instead of the row overflowing: put the five cards in widgets.FlowLayout so they wrap to a second line when 910 px is not there (they are already QSizePolicy.Expanding, so a wrapped row still fills), or re-flow to 3+2 under a width threshold. Setting the scroll area's horizontal policy to ScrollBarAlwaysOff alone would only trade the scrollbar for a hard clip.

### 5. [fixed-heights] Opening the grid's filter bar crushes the whole floating header to half height
    vrgallery/gridpage.py:552   confidence high
    WRONG: PageHead is a floating overlay, not a layout child: its geometry is only ever written by PageHead.place() (widgets.py:1148-1176, geo height = self.sizeHint().height()). set_extra_row (widgets.py:940-944) puts the filter bar into the header's own QVBoxLayout, so showing it raises PageHead.sizeHint() from 62 to 112 -- but nothing calls place(), so the widget stays 62 px tall and its layout has to squeeze bar(min 54) + filter row(min 50) into 62 - 8 margin. Measured live at 1500x950: before the click bar=QRect(0,0,1166,54), extra hidden; immediately after, head geo is still (8,0,1166,62) while sizeHint().height()==112, bar collapses to 27 px and the filter row to 27, and each QDateEdit is laid 
    SEEN:  Photos or Favorites page, click the funnel 'More filters' button in the header. Every window size, every time; the header stays crushed until the window is resized or the grid is scrolled more than 60 px. Pressing Apply (which calls refresh()) is what happens to fix the spacer, so a user who opens the panel and reads it without applying sees both halves.
    FIX:   In _toggle_filters, after self.filter_bar.setVisible(on), call self.headwrap.place() (or self._position_overlays(), which also re-places the sticky day header and the selection bar), and re-issue the top-gap reservation -- set the spacer row to self.head_height() again (e.g. self.refresh(), or a set_top_gap on the model) so the grid content still starts below the now-taller header.

### 6. [fixed-heights] Lightbox info panel lays its people chips past the bottom of the window with no way to scroll
    vrgallery/lightbox.py:782   confidence high
    WRONG: self.panel is a plain QFrame with setFixedWidth(292) (line 511) in the lightbox's root QHBoxLayout -- so its height is the window's, and there is no QScrollArea anywhere in it. 'WHO WAS THERE' adds one chip per player in the instance with no cap, and the chips_holder FlowLayout wraps them into a 256 px column. Measured against the real library (busiest photo has 61 players) at 1500x950: panel height 950, panel layout sizeHint 1877; chips_holder is given QRect(18,311,256,621) while its own heightForWidth(256) is 1506. 34 of the 61 chips are positioned below y=950 and are clipped away by the frame -- the last visible one is cut in half at the window edge, and the rest simply do not exist on sc
    SEEN:  Open the lightbox (info panel on, the default) on any photo taken in a busy public instance -- more than about 25 people in the session. At 1500x950 27 of 61 names show; a shorter window or a long world name / a filled 'TAGGED IN FRAME' section pushes the chips lower and loses more. No scrollbar appears, so the hidden names are unreachable.
    FIX:   Either wrap the panel's contents in a QScrollArea (the panel is already a fixed-width column, so a vertical scroll costs nothing), or cap the chips the way Sessions and Moments do -- players[:N] plus a '+{len(players)-N}' label that opens the session.

### 7. [hairlines] Timeline rail: every month/year tick drawn at an integer y with antialiasing on, so each renders as two rows at half strength
    C:\Users\Erik\Desktop\project\vrchronicle\vrgallery\gridpage.py:208   confidence high
    WRONG: _y_for() returns a float; int(y) truncates it to a whole coordinate. A cosmetic 1-px pen is centred ON that coordinate, so the stroke covers y-0.5 .. y+0.5, and with Antialiasing enabled (set at gridpage.py:190 for this cached pixmap) the raster engine splits the coverage 50/50 across rows y-1 and y instead of filling one row. Measured in the shipped render ux2/all.png, column x=1478: every month tick is two rows, (31,35,47) then (30,35,46), on a (13,15,21) background. border2 is #303748 = (48,55,72), and (48+13)/2 = 30.5 -- the intended colour never appears on screen anywhere in the rail. The year ticks are the same: (26,30,41)/(25,29,41), the exact 50% blend of border #262c3d = (38,44,61).
    SEEN:  Always, on every grid page whose library spans more than one month -- Photos, Favorites, and every world/person/album drill-down. Worst at 100% and 150% display scaling; at exactly 200% a 1-logical-px line at an integer coordinate happens to cover two device rows fully, so the smear disappears and it looks correct there.
    FIX:   Draw the ticks on the half-pixel: p.drawLine(QPointF(right - 5, int(y) + 0.5), QPointF(right, int(y) + 0.5)) at line 208 and the same at line 216, matching StickyDay's r.bottom() - 0.5 at line 309.

### 8. [hairlines] Lightbox ImageView.paintEvent never enables Antialiasing, so every tag box, name pill and the people badge steps
    C:\Users\Erik\Desktop\project\vrchronicle\vrgallery\lightbox.py:197   confidence high
    WRONG: SmoothPixmapTransform is set but Antialiasing is not, and QPainter starts with it off. Every shape _paint_tags then draws is curved and lands on fractional coordinates (they come from _image_rect(), a float fit-scale): the dashed live box (drawRoundedRect radius 6, line 228), each face-tag outline drawn twice (a 3.4/4.4-px dark under-stroke and a 1.6/2.4-px white stroke, lines 246 and 250), the name pill (radius 8, line 263) and the people badge (60x30 at radius 15, i.e. a full pill, line 177) together with its 1-px white border, QPen(QColor(255,255,255,60), 1) at line 175. With the hint off the engine hard-thresholds coverage, so those curves come out as whole-pixel staircases and the hairl
    SEEN:  Any photo that has face tags, at every window size and every DPR: hovering a person (their box and name pill), the 'show everyone' mode, tagging mode's live dashed box, and the people badge, which is on screen for every tagged photo whenever tagging is off.
    FIX:   Add p.setRenderHint(QPainter.Antialiasing, True) next to the SmoothPixmapTransform hint at lightbox.py:198, as every other paintEvent in the package does. The photo blit is unaffected -- it is SmoothPixmapTransform that governs it.

### 9. [hairlines] Drag ghost's rim is stroked on the pixmap boundary: half the hairline is lost, the rest leaks onto the transparent pad
    C:\Users\Erik\Desktop\project\vrchronicle\vrgallery\gridmodel.py:928   confidence medium
    WRONG: The stroke is centred on the path, so it spans -0.5..0.5 along the top and left edges and 95.5..96.5 along the bottom and right: no half of it lands wholly inside a pixel. Replayed exactly in an offscreen probe (scratchpad/probe_drag.py): the top-edge pixel (50,0) comes back (78,81,93) over a (40,44,58) fill, which is 45/255 white -- exactly half the 90 alpha asked for -- and the outer half falls off the pixmap and is lost. On the bottom and right the lost half instead lands in the transparent padding as (255,255,255,45), a detached one-pixel ghost stripe sitting outside the tile. So the ghost's border is half as bright as intended on all four sides and has a faint stray outline hanging off 
    SEEN:  Every drag of photos out of the grid (to Explorer, Discord, anywhere) at 100% scaling -- the picture under the cursor for the whole gesture. Cosmetic rather than structural: the rim is dim rather than missing, which is why it has not been noticed.
    FIX:   Stroke an inset path: build the outline from body.adjusted(0.5, 0.5, -0.5, -0.5) (keeping the fill/clip path on body), so the hairline covers rows 0 and 95 exactly.

### 10. [text] Header title is drawn clipped top and bottom: the bar is sized before fit() decides whether the subtitle is shown
    vrgallery/widgets.py:1163   confidence high
    WRONG: geo's height comes from self.sizeHint(), which is the bar's layout wish for the CURRENT subtitle visibility -- and the subtitle's visibility is decided by fit(), two lines LATER. So the header is always sized for the previous fit()'s answer. When a widening fit() gives the dropped subtitle (_optional prio 0.5, the first thing dropped and the last thing taken back) its place again, the widget keeps the old, one-line-shorter height; the bar is then forced below its own layout minimum and the squeeze is passed down to lab_title. Nothing calls place() again: lab_sub.setText -> on_change -> _apply_title_width only touches WIDTH, and allow() (gridpage.configure) calls fit() with no place() either,
    SEEN:  Cleanup page at the default 1500x920 window, in the shipped render: ux2/cleanup.png's glass bar occupies rows 36-83 (48px) while every other page's bar is rows 36-99 (64px), and "Cleanup" leaves only 14 rows of ink (46-59) where "Statistics" -- which has no descender at all -- leaves 16 (53-68); the C/l tops and the p descender are shorn off. Cleanup is the page that trips it here because its five mode pills make the
    FIX:   Re-place after fitting: either call fit() before computing geo, or end place()/fit() by re-reading self.sizeHint().height() and resizing the widget (guarding the recursion with the _placed key). Sizing geo from self._open rather than sizeHint() would also keep it consistent with reserved(). Belt and braces: have ElidedLabel.paintEvent draw into self.rect() expanded to at least fm.height(), so a squeezed label loses i
